#!/bin/sh
# AppImage entry point — finds the bundled `kashot` binary and runs it.
# Wrapped via linuxdeploy in the build-rust.yml CI workflow.

HERE="$(dirname "$(readlink -f "$0")")"
exec "$HERE/usr/bin/kashot" "$@"
