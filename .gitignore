# Build output (C#)
**/bin/
**/obj/
*.user
*.suo

# Build output (Rust)
**/target/
Cargo.lock.*

# Rust packaging staging — only the kashot-rs/dist/ subdir, NOT root /dist/
# (root /dist/ holds package-channel metadata: winget, choco, scoop, brew, etc.)
/kashot-rs/dist/

# Visual Studio
.vs/
*.swp

# Published artifacts
publish/
*.pdb
*.msi
*.wixpdb
preview.png

# OS
Thumbs.db
.DS_Store

# Editor
.idea/
.vscode/

# Local dev
*.log

# Local-only files: deployment runbooks, DNS / domain notes,
# anything specific to the maintainer's environment. Never push.
.local/
*.local
*.local.*
DEPLOY.md
DEPLOY-*.md
deploy.md
SECRETS.md
.env
.env.*

# Claude Code session config
.claude/
